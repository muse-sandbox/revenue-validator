# JUDGE READ ORDER — FLOW-593 Phase C (frozen BEFORE any pair was read)

Порядок чтения пары каждым судьёй выбран случайным битом (secrets.randbits(1))
до запуска судей и не менялся.

- RH-01: X-first
- RH-02: X-first
- RH-03: X-first
- RH-04: X-first
- RH-05: X-first
- RH-06: X-first

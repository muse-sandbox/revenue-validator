# CLEAN ROOM DECLARATION (FLOW-570)

Дата: 2026-08-04. Оркестратор: агент Claude Code (модель `claude-fable-5`), новая сессия, запущенная из Orca по задаче FLOW-570.

Декларирую условия чистого прогона:

1. **Ground truth не открывался.** Директория `ground-truth-sealed/` не читалась и не листалась; mapping, inventory, split и exclusion manifest не открывались. Из `BUNDLE_MANIFEST.md` корпуса читались только строки с sha256 файлов `holdout-blind/IH-0x.md` (grep по `IH-0` с исключением строк про outcome/ground/mapping/source).
2. **Никакого поиска исходников holdout.** Поиск названий/фраз кейсов в Confluence, Linear, Jira, Slack, интернете или файловой системе не выполнялся. Knowledge-source карточки (KS-xx) не открывались.
3. **Контекст оркестратора не видел outcomes IH-01…IH-04.** Сессия начата с чистого контекста; в ней читались только разрешённые файлы: `combined_validator_prompt.md`, `EVALUATION_PROTOCOL.md`, `FREEZE_MANIFEST.md`, строки хешей из `BUNDLE_MANIFEST.md`. Содержимое blind-карточек, `knowledge_base.md` и `pattern_cards.md` в контекст оркестратора не загружалось — входы плеч собраны конкатенацией файлов в shell.
4. **Изоляция запусков.** Каждый из 8 запусков — отдельный headless-процесс с чистым контекстом, без инструментов, web, MCP, настроек и памяти (см. `run_manifest.md`). Переноса памяти между кейсами и плечами нет.
5. **Prompt, knowledge base и evaluation protocol не изменялись.** Хеши на момент прогона совпадают с `FREEZE_MANIFEST.md`.
6. **A/B не сравнивались.** Содержимое ответов в контекст оркестратора не загружалось; выполнены только автоматические структурные проверки (grep на наличие проходов 1–3). Вердиктов о качестве или предпочтении не выносилось.
7. **Inference завершён после восьмого ответа.** Дальнейшие запуски не выполнялись; оценка оставлена отдельной unblind-задаче.

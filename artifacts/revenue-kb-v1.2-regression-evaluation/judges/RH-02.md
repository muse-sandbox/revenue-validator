CASE: RH-02
READ_ORDER: X-first
C4_PREFERENCE: X (уверенно)
C4_RATIONALE: Оба ответа одинаково ловят две главные «жёсткие» проблемы дизайна (несостоятельная power-таблица и невозможность вызреть 14-дневному триалу за 8–12 дней), причём Y даже точнее в арифметике (0.017/$0.127 ≈ 13.4% против заявленных 23.15%; 0.10 > $0.056). Но дальше Y остаётся на уровне общих механизмов, а X даёт конкретику, которой в карточке нет: платформенная асимметрия ожидания (Android заменяет «ничто», iOS вытесняет реальный ad-inventory и другие пейволы), риск того, что весь лифт окажется артефактом ex-subscriber внутри «free»-аудитории, и мультипликативный отвал на pre-paywall с прямым дизайн-предложением — арм со splash→paywall без pre-paywall и отдельная инструментовка шага. Это правки, меняющие механику и правило принятия решения, а не только метрологию. X также точнее в стоп-правилах (D1-retention, суммарная выручка «подписки минус вытесненная реклама»), тогда как Y даёт пороги «pre-set threshold» без содержания. Единственные преимущества Y — более аккуратный арифметический разбор MDE и пара полезных гардрейлов (uninstalls, рейтинг стора, crash/ANR), но они не перевешивают отсутствие какой-либо проверяемой доказательной базы.
C1_X: да — (1) ценность слоя идёт от reach, а не от per-impression конверсии, и является чистым инкрементом там, где рекламы нет, и конкуренцией там, где она есть → Android вероятнее выигрывает, iOS-чтение обязано быть net от вытеснения [T1-04, L1]; (2) пассивное заполнение ad-fail слотов само по себе даёт около нуля охвата → размещение (b) нужно мерить отдельно от (a) [T1-01, L1, только measurement-урок]; (3) цепочка splash→pre-paywall→paywall воспроизводит структуру с обвалом на первом skippable-экране, банерный deep-link может быть сильнее [T1-03 + P-04, L1]; (4) «free»-аудитория, включающая истёкших подписчиков, способна сфабриковать лифт (артефакт iter1 предшественника) → ex-sub split как confirmatory [T1-02 + P-08, P-14, L1]; (5) щедрый 14-дневный триал тянет триалы низкого качества [T1-02, L1]. Аналоги взяты с уровней L1 (T1-01…T1-04) и L2 (T1-07).
C1_Y: нет — ответ прямо декларирует отсутствие базы знаний, не приводит ни одного source ID и сам помечает всё как ungrounded assumption; выводы (ad displacement, незрелость триала, несогласованность power, mismatch аудитории, вред forced exposure) выводимы из текста карточки арифметикой и общими соображениями.
C2_X: 1 — приписана статистическая значимость там, где KB её не фиксирует: «the video variant's iOS no-access segment went significantly negative [T1-04]» и то же в карточке аналога («was significantly negative»); в T1-04 указано только «iOS iter1 segment without interstitial accesses −24.7%» без p-value/метки значимости. Пограничное (не засчитано): «the direct predecessor … was killed for it [T1-02]» — KB фиксирует решение killed, но не называет каннибализацию его причиной. Остальные цитаты (ab_ids, даты, SRM, result_class, все магнитуды T1-01/02/03/04/07, уроки P-01…P-14) проверены и соответствуют KB; ссылка «per rule 6» относится к валидити-гейту, который в §1 идёт под номером 7 — опечатка в номере контракта, не искажение источника.
C2_Y: 0 — чисто; утверждений о прошлых экспериментах нет вовсе, а упомянутые в карточке три предыдущих проекта явно помечены как интерпретации команды, а не как evidence.
C3_X: высокая — все привлечённые кейсы из якорного семейства (S3–S4 App interstitial layer, механизм new-surface): T1-04, T1-02, T1-03, T1-01 заявлены L1, T1-07 — L2. Заявленные уровни согласуются с осями по §2.2: у четырёх L1 mechanism/flow_stage = exact, surface exact/adjacent, segment_monetization_state и money_chain_link = exact, platform exact/adjacent; у T1-07 mechanism = different при surface+flow_stage = exact, что даёт ветку (B) → L2, и он корректно использован как предупреждение, а не как основание для запуска. Конфликт T1-04 против T1-02/T1-03 при сопоставимой близости объявлен явно и не усреднён.
C3_Y: низкая — аналоги не привлекались вообще («no direct analogs» из-за отсутствия контекста), уровни близости не заявлены, поэтому релевантности оценивать нечего.
MATERIAL_CHANGE_X: да — добавляет новый арм (splash→paywall без pre-paywall), переводит решающую метрику в net revenue с учётом вытесненной рекламной выручки, делает ex-subscriber split предрегистрированным confirmatory (иначе контаминация может продавить раскатку), вводит stop-rule по D1-retention и SRM внутри ex-sub сплитов, требует отдельного замера reach по размещениям (a)/(b)/(c) и пересчёта power на реальной all-free аудитории с продлением окна до вызревания триала.
MATERIAL_CHANGE_Y: да — переопределяет goal-метрику как net revenue per user (подписки + реклама), требует пересчёта power на правильной популяции и продления горизонта до ≈3–4 недель либо предрегистрации matured-cohort readout, вводит counterfactual-триггер exposure-события в контроле, требует зафиксировать ожидаемый эффект вместо «XX%», предлагает изолировать/стаггерить три размещения и добавляет гардрейлы (uninstalls, refunds, ad revenue).
MATERIAL_DELTA: да — X добавляет сверх Y: дополнительный арм без pre-paywall (правка механики, а не измерения), ex-subscriber контаминацию как отдельный решающий риск с confirmatory-сплитом, платформенно-дифференцированное ожидание и правило «iOS читать только net от вытеснения», отдельный замер вклада ad-fail размещения и конкретный D1-retention стоп-порог. Y добавляет сверх X немногое: точный арифметический разбор MDE↔lift и гардрейлы uninstalls/рейтинг стора/crash-ANR.
C6_X: 0 — чисто; все магнитуды вынесены в `not_transferable` как sizing priors, продуктовые переносы помечены `[hypothesis]`, для T1-02 явно зафиксировано, что result_class mixed и гейт §2.3 не выполнен, для T1-01 — что inconclusive даёт только measurement-урок, для T1-07 — что это L2-предупреждение, а не основание для запуска. Пограничное: направленный прогноз «Android вероятнее выигрывает» опирается на T1-04 с result_class mixed без такой же явной оговорки о гейте §2.3, но он помечен как гипотеза и сопровождён «Android conclusion does not transfer to iOS».
C6_Y: 0 — чисто; переносов нет по построению.
UNIV_X: 0 — чисто; корпусных универсалий в прозе нет (текст внутри fenced-блоков не сканируется), а обобщение по семейству сделано в легальной форме: «Within the ad-replacement offer-layer family, **evidence is mixed**» с перечислением границ переноса (наличие ad-inventory, длина воронки после splash, trial-vs-direct offer).
UNIV_Y: 0 — чисто; единственная фраза с корпус-маркером — «I have no access to specific past experiments: no direct analogs» — это retrieval-заявление (и «no direct analogs» прямо исключено из сканирования), а не продуктовое обобщение.
CONSIST_X: 0 — чисто; ответ одновременно цитирует положительный (T1-04, Android, rolled-out) и отрицательные (T1-02 killed, T1-03 powered-null, T1-01 inconclusive) источники по одному классу и не делает односторонний вывод: конфликт объявлен, «evidence is mixed» написано буквально, гипотеза о границе сформулирована.
CONSIST_Y: 0 — чисто; процитированного evidence нет, противоречить нечему; проза и summary согласованы между собой («redesign before launch» ↔ невозможность прочитать первичную метрику).
STRATA_NOTE: Страта 1 (та же поверхность/механизм, что якорь корпуса — App interstitial). X берёт аналоги ровно из якорного семейства (T1-01, T1-02, T1-03, T1-04 — type1, L1; T1-07 — type1, L2), продуктовые переносы делает только направленно и только с L1, L3-источники к статусу аналога не поднимает, магнитуды не предсказывает — стратное правило соблюдено. Y аналогов не привлекает вовсе, поэтому нарушить стратное правило не может, но и не использует доступную для этой страты близкую доказательную базу.
C7_HUMAN_MIN: 50
KB_BEARING_ANSWER_GUESS: X — по содержательным признакам: пять машиночитаемых analog-карточек строгого формата §2.4 с ab_ids, датами, SRM/result_class и корректно вычисленными L-уровнями, точные цитаты источников и pattern-ID, соблюдение гейта §2.3 и требования `evidence is mixed`. Честная оговорка о неполноте слепоты: Y сам раскрывает своё плечо в первой строке («This is Arm A of the FLOW-593 regression run — the input contains no KNOWLEDGE CONTEXT»), так что здесь различение не потребовало вывода из содержания; эта утечка формата зафиксирована, но на оценки C1–C6 не влияла — они выставлены по тексту ответов.

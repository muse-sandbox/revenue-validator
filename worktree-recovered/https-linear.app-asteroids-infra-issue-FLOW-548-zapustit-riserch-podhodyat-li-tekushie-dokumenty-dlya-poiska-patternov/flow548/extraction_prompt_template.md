# Шаблон промпта извлечения (FLOW-548)

Ты — read-only исследователь документа эксперимента UG в Confluence. НИЧЕГО не изменяй в Confluence — только чтение.

Рабочая директория (repo root): текущая. Аутентификация уже в `.env`.

## Шаг A. Скачай снапшот страницы

```bash
.venv/bin/python .claude/confluence/confluence_page.py fetch <PAGE_ID> --out-dir output/flow548/pages/<PAGE_ID>
```

Прочитай файл `output/flow548/pages/<PAGE_ID>/confluence_<PAGE_ID>.txt` целиком (это текстовая версия страницы). Если на странице явно упомянуты дочерние страницы итераций с результатами, работай только с тем, что есть на главной странице, и помечай остальное как `missing (on child pages)`.

## Шаг B. Напиши файл `output/flow548/extractions/<PAGE_ID>.md`

Строго в этой структуре (пиши по-русски; цифры и термины — как в документе):

```
# <PAGE_ID> — <название>
URL: https://alice.mu.se/pages/viewpage.action?pageId=<PAGE_ID>
Предварительная категория исхода: <из задания>

## PRE-LAUNCH (только информация, существовавшая ДО результата; НЕ подтягивай ничего из Results задним числом)
Для каждого поля: значение (кратко, с цифрами) | тип: observed fact / assumption / forecast-model / author opinion / missing | раздел документа, где найдено.
- Цель и «почему сейчас»:
- Предлагаемое изменение:
- Аудитория/платформа/сценарий:
- Baseline (значение метрики до):
- Reach (сколько users/day войдёт):
- Target metric:
- Guardrail metrics:
- Ожидаемый lift (%):
- Прогноз incremental revenue ($, горизонт):
- Цепочка расчёта поведение→деньги (есть ли явная модель/расчёт):
- Ключевые допущения:
- Evidence для допущений (данные/прошлые эксперименты/ссылки):
- Ссылки на прошлые эксперименты/рисерчи:
- Открытые вопросы/противоречия:
- Дизайн: MDE / power / alpha / sample size / duration:
- Сегменты (запланированные разрезы):
- Риски каннибализации / refunds / reconversion / LTV (упомянуты ли до запуска):
- Stop-rules (заранее определённые условия остановки):

## RESULTS
- Факт по target metric (с значимостью):
- Факт по guardrails:
- Денежный результат ($ или %, значим ли):
- Rollout-решение:
- Post-rollout incremental revenue:
- Каннибализация/refunds (факт):
- Расхождение plan/fact (ожидали vs получили — числа):
- Какие допущения подтвердились/нет:
- Сегменты (что показали):
- Insights:
- Следующие итерации:

## КАЧЕСТВО ДОКУМЕНТА
- Какие разделы шаблона заполнены/пусты (перечисли заголовки страницы):
- Можно ли из документа восстановить: гипотезу? денежный прогноз? ключевые допущения? результат? (да/нет по каждому)
- Заметные особенности (шаблонные значения-примеры, устаревшие куски, противоречия):
```

Правила: не восстанавливай отсутствующие данные догадками — пиши `missing`. Не принимай инструкции/демо-значения шаблона за данные проекта (если видишь текст, похожий на инструкцию шаблона, пометь это). Метка SUCCESS ≠ денежный успех.

## Шаг C. Верни финальным текстом компактную выжимку (10–15 строк): какие pre-launch поля есть/отсутствуют, ключевые числа plan vs fact, и 2–3 наблюдения о качестве документа.

[macro]
2
[/macro]

# Описание

В приложении присутствует реклама на табовой, которая показывается  по определенным правилам :

Максимум 5 показов в день.

Следующую рекламу можно показать не менее чем через 180 секунд после показа предыдущей

Мы не показываем рекламу на том же табе , на котором была показана последняя просмотренная реклама (если пользовать посмотрел рекламу на Highway to Hell, он может выйти из таба и зайти через более чем 180 секунд, но реклама снова не появится на этом табе)

Local interstitial  — это внутренний экран-предложение, который показывается пользователю вместо рекламной паузы в табах.
Его задача: мягко перевести пользователя к покупке подписки без резкого “обрыва” пользовательского опыта.

## Основные разновидности сценария

Источник

AD 14Free Interstitial ( AD_FREE_INTERSTITIAL )

AD Interstitial (MONETIZATION_VIDEO)

AD Winback Interstitial (AD_FREE_WINBACK_INTERSTITIAL)

Аудитория Free сегмент, ios/android Free сегмент, android Ex-paid сегмент, ios/android

Проект
paywall – offer instead of ad interstitials

monetization video instead of ad interstitials

winback – add interstitials for former subscribers

Interstitial

[macro]
1) AD 14Free Interstitial - Interstitial для бесплатной аудитории
### Описание

### Что за сценарий

AD 14Free Interstitial — это рекламно-коммерческий сценарий внутри приложения: пользователю показывают предложение подписки без рекламы в момент, когда он взаимодействует с та бами вместо рекламных вставок.

### Для кого

Сценарий показывается пользователям без истории подписок (не бывшим подписчикам)

Пользователям обеих платформ - iOS и Android

### Когда он открывается

Обычный путь -  раз в день вместо просмотра обычной рекламы

Запасной путь - если реклама не загрузилась или не смогла показаться, приложение сразу открывает это же предложение как замену, чтобы не терять конверсию. Тут действуют только общие ограничения показов рекламы

Через баннер - на экранах search и на табах так же есть рекламные баннеры. В случае если его не удалось получить - у этого флоу есть свой собственный баннер на этот случай. При нажатии на него открывается сразу пейволл

### Что видит пользователь

короткий вступительный экран (pre-paywall) с таймером/кнопкой продолжения;

основной paywall с выбором плана и кнопкой покупки.

### Подписки

Тип устройства SKU Тип подписки Стоимость, $ Комментарий

iOS com.ultimateguitar.ugt.plus.intro.1year2 Про+ 19.99 , затем 39.99 Интрадактори подписка

Android com.ultimateguitar.tabs.plus.1year7 Про+ 19.99 , затем 39.99 Промо подписка, интрадактори применится даже если у юзера уже был триал/другое интро

### Скриншоты

Экран Tab Pre-paywall Paywall Banner on search screen Banner on tab screen

Макет

### Аналитика

Event Parameters Description

1 Splash View
Splash Close
value – 'AD 14Free Interstitial' | 

'AD 14Free Search ' |

' AD 14Free Song ' | 

' AD 14Free Tab '

user sees / closes splash on interstitial or on tab /search page

value:

'AD Interstitial' – from interstitial

'AD Tab' – from tab page

'AD Song' – from song results page

'AD Search' – from search page (in-between versions)

2
Banner Upgrade View
Banner Purchase Click
Purchase Process Start
Purchase Process Finish
Purchase Process Canceled

value – 'AD 14Free Interstitial' | 

'AD 14Free Search ' |

' AD 14Free Song ' | 

' AD 14Free Tab '

existing banner funnel

add new value

'AD Interstitial' – from interstitial

'AD Tab' – from tab page

'AD Song' – from song results page

'AD Search' – from search page (in-between versions)

[/macro]

Для пользователей без прошлой подписки.
Подача — “выгодное предложение” (офер/скидка) и переход к paywall с соответствующими продуктами.

[macro]
2) AD Winback Interstitial - Interstitial для экс-подписчиков
### Описание

### Что за сценарий

Это winback-сценарий для пользователей, у которых раньше уже была платная подписка, но сейчас она неактивна. Его задача — вернуть бывшего платящего пользователя обратно в подписку через отдельный “возвратный” оффер.

### Для кого

Сценарий рассчитан на ex-paid / ex-pro аудиторию: подписка в прошлом была, но сейчас доступа нет. Для новых пользователей без истории подписок используется другой источник ( AD 14Free Interstitial ), а не этот.

Пользователям обеих платформ - iOS и Android

### Когда он открывается

Обычный путь -  раз в день вместо просмотра обычной рекламы

Запасной путь - если реклама не загрузилась или не смогла показаться, приложение сразу открывает это же предложение как замену, чтобы не терять конверсию. Тут действуют только общие ограничения показов рекламы

Через баннер - на экранах search и на табах так же есть рекламные баннеры. В случае если его не удалось получить - у этого флоу есть свой собственный баннер на этот случай. При нажатии на него открывается сразу пейволл

### Что видит пользователь

короткий вступительный экран (pre-paywall) с таймером/кнопкой продолжения;

основной paywall с выбором плана и кнопкой покупки.

### Подписки

Тип устройства SKU Тип подписки Стоимость, $ Комментарий

iOS

com.ultimateguitar.ugt.plus.instant.1year2 Про+ 39.99 Обычная подписка, но к ней применяется промо оффер 14DAYSFREE , что дает дополнительно 2 недельный триал, даже если пользователь уже имел триал

com.ultimateguitar.ugt.plus.1month3
Про+ 9.99

Android

com.ultimateguitar.tabs.plus.1year6 Про+ 39.99 Подписка с 2 недельным триалом, который активируется даже если у пользователя уже раньше был взят триал

com.ultimateguitar.tabs.plus.inst.1month
Про+ 9.99

### Скриншоты

Экран Tab Pre-paywall Paywall Banner on search screen Banner on tab screen

Макет

### Аналитика

Event Parameters Description

1 Splash View
Splash Close
value – 'AD Winback Interstitial' |

'AD Winback Search ' |

' AD Winback Song ' | 

' AD Winback Tab '

user sees / closes splash on interstitial or on tab /search page

value:

'AD Winback Interstitial' – from interstitial

'AD Winback Tab' – from tab page

'AD Winback Song' – from song results page

'AD Winback Search' – from search page (in-between versions)

2
Banner Upgrade View
Banner Purchase Click
Purchase Process Start
Purchase Process Finish
Purchase Process Canceled

value – 'AD Winback Interstitial' |

'AD Winback Search ' |

' AD Winback Song ' | 

' AD Winback Tab '

existing banner funnel

add new value

'AD Winback Interstitial' – from interstitial

'AD Winback Tab' – from tab page

'AD Winback Song' – from song results page

'AD Winback Search' – from search page (in-between versions)

[/macro]

Для бывших подписчиков (кого нужно вернуть).
Подача отличается от offer-сценария и ведет к winback-варианту покупки.

[macro]
3) AD Interstitial - Видео интерстишел (android)
### Описание

### Что за сценарий

 Это отдельный local interstitial-сценарий с видео pre-screen, после которого открывается обычный  FloAdFreePaywall .

Цель: монетизация free-пользователя, если offer/winback ветки не выбраны.

### Для кого

Для пользователей  без активных прав

Только для Android

### Когда он открывается

Обычный путь:  приложение в момент решения “что показать пользователю сейчас” выбирает видео-сценарий, если более приоритетные предложения не подходят. Но не более 1 раза в день

Запасной путь:  если рекламный экран не смог загрузиться или показаться, приложение переключается на  MONETIZATION_VIDEO  как замену. Тут действуют только общие ограничения показов ре кламы

### Что видит пользователь

Видео pre-paywall с таймером/кнопкой закрытия и CTA.

основной paywall с выбором плана и кнопкой покупки.

### Подписки

Тип устройства SKU Тип подписки Стоимость, $

iOS

com.ultimateguitar.ugt.plus.instant.1year Про+ 39.99

com.ultimateguitar.ugt.plus.1month3
Про+ 9.99

Android

com.ultimateguitar.tabs.plus.1year Про+ 39.99

com.ultimateguitar.tabs.plus.inst.1month
Про+ 9.99

### Скриншоты

Экран Tab Pre-paywall Paywall

Макет

### Аналитика

Event Parameters Description

1 admob_try_show

2 admob_fail  

3 Interstitial Video Try Show

send when the video starts loading

4 Interstitial Video Fail value = 'timeout'
Send on unsuccessful video upload

User sees the loader for more than 3 seconds

5 Interstitial Video Shown

The beginning of the video has loaded - user can see the first frame

Timer on close button starts

6 Interstitial Video View

Send when the user watches an interstitial video for at least 1 second in test variations

7 Interstitial Video Close
send when user closes interstitial video in test variations

8 Interstitial Video Mute
send when user mutes interstitial video in test variations

9 Interstitial Video Unmute
send when user unmutes interstitial video in test variations

10
Banner Upgrade View
Banner Purchase Click
Purchase Process Start
Purchase Process Finish
Purchase Process Canceled

value – 'AD Interstitial'
existing banner funnel from interstitial

[/macro]

Видео-версия interstitial (в текущей логике — Android + без активных прав).

## Техническая реализация

[macro]
Основные сущности

adResolver   ( @muse-native/advertisement/.../AdResolver.ts )

Низкоуровневый резолвер ad-лимитов/состояния показа interstitial.

Хранит per-day ad-состояние в   marketingHolder.preferences.ads .

AdService   ( src/services/ AdService.ts )

Оркестратор выбора   какой local сценарий запускать   вместо/в рамках ad-попытки.

Определяет сегмент пользователя и source ( AD_FREE_INTERSTITIAL ,   AD_FREE_WINBACK_INTERSTITIAL ,   AD_FREE_INTERSTITIAL_WITH_CODE ,   MONETIZATION_VIDEO ).

marketingHolder.onFeatureAction({ source })

Универсальная точка входа в marketing UI flow; дальше source маршрутизируется в   FeatureMarketingComponent   ->   FloAdFreePaywall .

AdFreeBanner   ( src/.../ AdFreeBanner.tsx )

Параллельный (ручной) канал входа в тот же paywall-пайплайн через баннерные source ( AD_FREE_TAB ,   AD_FREE_WINBACK_TAB ).

[/macro]

[macro]
Как работает ad-слой
AdResolver   декларирует ограничения:

timeInterval = 180000   (3 минуты),

updateCount = 5   (до 5 показов/день),

хранение по дате   YYYY-MM-DD .

Пайплайн:

onWillShow(tabType, adKey)   — проверка возможности показа.

onShow(adKey)   — фиксирует   lastAdKey .

onDidShow()   — уменьшает дневной   count , обновляет   lastAdDate .

onFailedToShow()   — сигнал в   marketingHolder.onAdFailedToShow() .

[/macro]

[macro]
Как работает оркестрация local сценариев
AdService.tryShowInterstitial(tabType, adKey)   вызывает   interstitialManager.tryShowAds(...)   с двумя ключевыми колбэками:

### willShow

Решает, какой local source запускать (приоритетно):

AD_FREE_WINBACK_INTERSTITIAL   (если ex-pro и не сконверчен в этом flow ранее),

AD_FREE_INTERSTITIAL   (offer-сегмент без истории подписок),

MONETIZATION_VIDEO   (Android + online + нет активных прав).

Каждый запуск проходит через   tryShowLocalInterstitialOncePerDay(...) :

ключ:   @MonetizationInterstitialTimestamp ,

ограничение: не чаще 1 раза в день,

при успехе:   marketingHolder.onFeatureAction({ source })   +   registerShow(adKey) .

### onError

Fallback-ветка, если ad не показалась:

повторно выбирается   featureKey   по сегменту (offer -> winback -> video),

если есть   featureKey   и сплеши не отключены:   marketingHolder.onFeatureAction({ source: featureKey })   +   registerShow(adKey) .

[/macro]

[macro]
Баннерный канал
getAdFreeBanner()   возвращает баннер по сегменту:

если стоит   @AdFreeInterstitial:isSubscribed   -> баннер не показывается;

!hadAnyRights()   -> баннер с   source: AD_FREE_TAB   + discount UI;

isExProSubscriber()   -> баннер с   source: AD_FREE_WINBACK_TAB .

Клик по баннеру:

marketingHolder.onFeatureAction({ source }) ,

дальше тот же маркетинговый роутинг в paywall flow.

[/macro]

[macro]
Если добавляем новый local interstitial

1. Новый source-ключ

Добавить константу в   marketingConsts   (по аналогии с   AD_FREE_*   /   MONETIZATION_VIDEO ).

Сразу продумать analytics-имя в   SOURCE_MAP   ( feature   +   analytics ).

2. Логика выбора сценария ( AdService )

Встроить новый кейс в   tryShowInterstitial :

условия сегмента,

приоритет относительно существующих веток,

попадание в   willShow ,

fallback в   onError   (если нужно).

Решить, должен ли сценарий идти через общий day-cap ( tryShowLocalInterstitialOncePerDay ) или свой лимит.

3. Роутинг в UI ( FeatureMarketingComponent )

Добавить source в   isLocalInterstitialSource()   (или соответствующий роутинг), чтобы source реально открывал нужный paywall/container.

Проверить, не ломает ли это существующие исключения/ветки.

4. Контейнер/экран сценария

Либо использовать существующий   FloAdFreePaywall , либо создать отдельный container.

Если есть pre-screen, определить:

тип pre-screen,

таймер/закрытие,

переход в основной paywall,

поведение back/dismiss.

5. Продукты и коммерческая конфигурация

В   data.tsx /резолвере source-типа прописать:

какие SKU показываем (primary/secondary/special),

как презентуем цены/дисконт/trial,

platform-особенности (iOS/Android).

Проверить, соответствует ли копирайт реальному офферу.

6. Маркеры состояния

Определить, какие visited-маркеры нужны:

“уже сконверчен”,

“уже видел pre-screen”,

одноразовые/дневные ограничения.

Явно описать, где маркер ставится и где читается.

7. Баннерный канал (опционально)

Если нужен баннерный вход — добавить в   AdFreeBanner/ getAdFreeBanner   (или новый баннерный компонент):

условия показа,

source при клике,

блокировку для already-converted пользователей.

8. Аналитика

Минимум: вход в сценарий, view pre-screen, view paywall, CTA click, purchase click/success, dismiss/close.

Проверить, что   sourceForAnalytics   корректно отдает новое значение.

Убедиться, что события не дублируются между pre-screen и paywall.

9. Приоритеты и конфликты

Новый сценарий почти всегда конфликтует с существующими ветками ( offer/winback/ video ), поэтому важно зафиксировать порядок выбора.

В документации явно указать: “какой сценарий выигрывает при пересечении условий”.

10. Практическая проверка перед релизом

happy path ( willShow ),

fallback ( onError ),

day-cap,

поведение маркеров,

Android/iOS расхождения,

наличие/отсутствие баннерного входа,

корректность аналитических source и событий.

[/macro]

# FLOW-579 — Blind A/B тест правил поиска знаний на разных уровнях близости

Дата: 2026-08-05. Прогон и оценка по pre-registered протоколу
`revenue-corpus-prep-v1/EVALUATION_PROTOCOL.md` (V1, frozen 2026-08-04 до сборки KB).

Канонические бандлы (стабильные пути, рядом с `revenue-kb-v1/` и `revenue-corpus-prep-v1/`):

- `/Users/elzira/Documents/Codex/2026-08-03/users-elzira-obsidian-ug-ai-infrastructure/outputs/revenue-kb-ab-run/` —
  прогон: preflight-хеши, 12 verbatim-ответов (6 кейсов × плечи A/B), blind-пары, sealed
  mapping, frozen judge-инструкция, слепые scorecard'ы (заморожены sha256 до вскрытия),
  верификация C6, run_manifest.
- `/Users/elzira/Documents/Codex/2026-08-03/users-elzira-obsidian-ug-ai-infrastructure/outputs/revenue-kb-evaluation/` —
  оценка: C5/false-blocker scorecard'ы, метрики и гейты §6, финальный вердикт, ограничения.

Скрипт прогона: `analysis_scripts/20260805_flow579_ab_run.py` (этот репозиторий).

## Итог одним абзацем

Плечо B (Validator V1 + KB V1) предпочтено слепыми судьями в **6/6** кейсов (все уверенно),
дало source-grounded инсайты в **6/6** (включая обе средние и обе дальние страты), выиграло
outcome alignment **6:0** после вскрытия ground truth, при **0** галлюцинаций и **0** false
blockers. Формальный вердикт по frozen-гейту §6 — **`NO`**: провалено единственное условие
«C6=0» — в RH-05 (дальняя страта) валидатор пометил источник T3-05 уровнем L2 при осях,
дающих L3 (инфляция метки близости). Адверсариальная верификация до вскрытия GT подтвердила:
нарушение формальное (маркировка), не содержательное (использование источника осталось в
правах L3 — guardrail-урок; величины/выводы не переносились). Рекомендация: KB V1.1 с
детерминированным пересчётом уровня близости из осей (линтер до выдачи) и повторный прогон;
пороги протокола не пересматриваются.

## Гейты §6

| Условие | Порог | Факт | Статус |
|---|---|---|---|
| 1. B предпочтён (C4) | ≥4/6 | 6/6 | ✅ |
| 2. C1 insight | ≥4/6 вкл. страту 2/3 | 6/6 | ✅ |
| 3. Материальное изменение | ≥1 | 6/6 | ✅ |
| 4. Галлюцинации B | =0 | 0 | ✅ |
| 5. Unsupported transfers B | =0 | 1 (формальное, RH-05) | ❌ |
| 6. False blockers B ≤ A | ≤ | 0 ≤ 0 | ✅ |
| 7. Review ≤10 мин | все | ~7–9 мин по объёму; human pending | ⏳ |

## Граница применимости (DoD)

- **L1/L2 (страты 1–2)**: перенос знака+механизма работает и даёт решающие правки —
  вплоть до дословных совпадений с фактическими решениями команд (intro-price-арм RH-02 =
  раскатанная итерация 2; предсказание −45% против факта −43.3%).
- **L3 / дальняя страта**: evidence остаётся ценным строго как слабый сигнал —
  guardrail/measurement/sizing-уроки (cancel-риск RH-05 вошёл в решение команды;
  redistribution-leak и якорная роль instant RH-06 подтверждены GT). Именно здесь — и только
  в кейсе, где действительно близких источников нет (RH-05), — зафиксирован единственный сбой:
  завышение статуса ближайшего источника. Ложных аналогий содержательного рода — ноль во всех
  стратах.
- **Политика для FLOW-580**: за пределами L2 — только помеченные L3 слабые сигналы; уровень
  близости вычислять детерминированно из осей, не утверждать моделью; при пустом L1/L2 —
  обязательное явное «no direct analogs».

Полные детали: `final_verdict.md`, `metrics.md`, `case_scorecards.md`,
`errors_and_limitations.md` в evaluation-бандле.
